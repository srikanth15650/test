package com.example.demo;

public class OrderCommandHandler {
    public void handle(CreateOrderCommand command) {
        // Validate business rules
        // Persist the order in the write database
    }
}